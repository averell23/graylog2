#
# Generated by Chef
#

general:
 external_hostname: "<%= (node[:ec2] && node[:ec2][:public_hostname]) || node[:fqdn] || "localhost" %>" # Used for example to generate permalinks. Don't add 'http://' or trailing slashes.
 date_format: "%d.%m.%Y - %H:%M:%S" # http://ruby-doc.org/core/classes/Time.html#M000298 (strftime syntax)

# Settings for stream subscription emails.
subscriptions:
  from: <%= node.graylog2.stream_alarms_email %>
  subject: "[graylog2] Subscription"

# Settings for stream alarm emails.
streamalarms:
  from: <%= node.graylog2.stream_subscriptions_email %>
  subject: "[graylog2] Stream alarm!"

